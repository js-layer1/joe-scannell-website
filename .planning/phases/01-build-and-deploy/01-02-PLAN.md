---
phase: 01-build-and-deploy
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - app/page.tsx
  - app/layout.tsx
  - app/favicon.ico
  - app/apple-icon.png
  - app/opengraph-image.png
  - components/Avatar.tsx
  - components/NameCard.tsx
  - components/LinkList.tsx
autonomous: false
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03
  - PAGE-04
  - PAGE-05
  - PAGE-06
  - PAGE-07
  - PAGE-08
  - DES-01
  - DES-02

must_haves:
  truths:
    - "Visiting the page shows a circular headshot above the name"
    - "The H1 reads exactly 'Joe Scannell'"
    - "The tagline reads exactly 'Founder, Layer One Group'"
    - "Four links are present with equal visual weight: Layer One Group, LinkedIn, Twitter/X, Email"
    - "The page is centered and readable on a 375px viewport without horizontal scroll"
    - "The design feels warm and personal, NOT like the Layer One Group brand"
  artifacts:
    - path: "components/Avatar.tsx"
      provides: "Headshot with next/image priority prop -- LCP element"
      exports: ["Avatar"]
    - path: "components/NameCard.tsx"
      provides: "H1 name + tagline subheading"
      exports: ["NameCard"]
    - path: "components/LinkList.tsx"
      provides: "Equal-prominence external links (4 links)"
      exports: ["LinkList"]
    - path: "app/page.tsx"
      provides: "Root page composing Avatar + NameCard + LinkList"
    - path: "app/favicon.ico"
      provides: "Browser tab icon"
    - path: "app/apple-icon.png"
      provides: "Apple touch icon"
    - path: "app/opengraph-image.png"
      provides: "Static 1200x630 OG image"
  key_links:
    - from: "app/page.tsx"
      to: "components"
      via: "imports Avatar, NameCard, LinkList"
      pattern: "import.*Avatar|import.*NameCard|import.*LinkList"
    - from: "components/Avatar.tsx"
      to: "public/headshot.jpg"
      via: "next/image src prop with priority"
      pattern: "priority"
    - from: "components/LinkList.tsx"
      to: "external URLs"
      via: "target='_blank' rel='noopener noreferrer'"
      pattern: "noopener noreferrer"

user_setup:
  - service: "browser"
    why: "Visual verification of warm aesthetic and mobile layout"
    dashboard_config:
      - task: "Open Vercel subdomain URL in a browser and resize to 375px width to check mobile layout"
        location: "Browser DevTools > responsive mode"
---

<objective>
Build all UI components (Avatar, NameCard, LinkList), implement the warm personal design system, and create all static asset files (favicon, apple-icon, OG image).

Purpose: This is the visual core of the site. The design must feel warm, personal, and distinct from Layer One Group's glass/gradient aesthetic.
Output: A fully styled, mobile-responsive page with all four links and the headshot, ready for the SEO layer to be added in Plan 01-03.
</objective>

<execution_context>
@/Users/joe/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-build-and-deploy/01-CONTEXT.md
@.planning/phases/01-build-and-deploy/01-01-SUMMARY.md

<interfaces>
<!-- Key patterns from Plan 01-01 that this plan builds against -->

From app/layout.tsx (created in 01-01):
```typescript
// Root layout with Geist font variable on <html>
// This plan adds NO metadata export -- that goes in Plan 01-03
// This plan only touches layout.tsx to add any needed body classes

import { Geist } from 'next/font/google'
const geist = Geist({ subsets: ['latin'], variable: '--font-geist', display: 'swap' })
```

From app/globals.css (created in 01-01):
```css
@import "tailwindcss";
/* Design tokens will be added in this plan */
```

From next.config.ts (created in 01-01):
```typescript
// output: 'export', images: { unoptimized: true }
// next/image works but does NOT optimize at runtime -- use local src
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Avatar, NameCard, and LinkList components with warm design system</name>
  <files>components/Avatar.tsx, components/NameCard.tsx, components/LinkList.tsx, app/globals.css, app/page.tsx</files>
  <action>
    Design intent (from user decisions -- LOCKED):
    - Warm off-white background, dark charcoal text, rounded elements, soft shadows
    - Small/subtle headshot -- text is primary
    - Spacious centered layout, content floats in the middle
    - Four links at equal prominence (no hierarchy)
    - Distinct from Layer One Group glass/gradient aesthetic -- personal, not agency

    Claude's discretion: exact layout (use a centered vertical stack -- avatar, name, tagline, then links as pill-shaped buttons), link style (rounded pill buttons with a soft border, subtle hover), accent color (use a warm stone/amber as the only accent -- a soft warm tone like #B8956A or similar), spacing scale.

    **1. Update `app/globals.css`** -- add design tokens after the @import:
    ```css
    @import "tailwindcss";

    :root {
      --font-geist: '';
      --color-bg: #FAF8F5;         /* warm off-white */
      --color-text: #2C2825;       /* dark charcoal */
      --color-muted: #6B6560;      /* warm mid-gray for tagline */
      --color-border: #E8E0D8;     /* warm light border */
      --color-accent: #B8956A;     /* warm amber accent -- subtle */
    }

    html {
      background-color: var(--color-bg);
      color: var(--color-text);
    }

    body {
      font-family: var(--font-geist), system-ui, sans-serif;
    }
    ```

    **2. Create `components/Avatar.tsx`:**
    ```typescript
    import Image from 'next/image'

    export function Avatar() {
      return (
        <div className="flex justify-center">
          <Image
            src="/headshot.jpg"
            alt="Joe Scannell"
            width={96}
            height={96}
            priority
            className="rounded-full object-cover shadow-sm ring-2 ring-[var(--color-border)]"
          />
        </div>
      )
    }
    ```
    `priority` is REQUIRED -- this is the LCP element. Without it, Core Web Vitals fail.

    **3. Create `components/NameCard.tsx`:**
    ```typescript
    export function NameCard() {
      return (
        <div className="text-center space-y-1">
          <h1 className="text-2xl font-semibold tracking-tight text-[var(--color-text)]">
            Joe Scannell
          </h1>
          <p className="text-sm text-[var(--color-muted)]">
            Founder, Layer One Group
          </p>
        </div>
      )
    }
    ```
    H1 text must be exactly "Joe Scannell" (no change -- this feeds the Person schema name field).
    Tagline must be exactly "Founder, Layer One Group".

    **4. Create `components/LinkList.tsx`:**
    Use placeholder URLs for LinkedIn and Twitter/X -- these will be confirmed and finalized in Plan 01-03. Use `href="#"` placeholders for now with a comment marking them for replacement.
    ```typescript
    const links = [
      {
        label: 'Layer One Group',
        href: 'https://layeronegroup.com',
        icon: '↗',
      },
      {
        label: 'LinkedIn',
        href: 'https://linkedin.com/in/joescannell', // PLACEHOLDER -- confirm slug in Plan 01-03
        icon: '↗',
      },
      {
        label: 'Twitter / X',
        href: 'https://twitter.com/joescannell', // PLACEHOLDER -- confirm handle in Plan 01-03
        icon: '↗',
      },
      {
        label: 'Email',
        href: 'mailto:hello@layeronegroup.com',
        icon: '→',
      },
    ]

    export function LinkList() {
      return (
        <nav aria-label="Links" className="flex flex-col gap-2 w-full max-w-xs">
          {links.map((link) => (
            <a
              key={link.label}
              href={link.href}
              target={link.href.startsWith('mailto:') ? undefined : '_blank'}
              rel={link.href.startsWith('mailto:') ? undefined : 'noopener noreferrer'}
              className="flex items-center justify-between px-4 py-3 rounded-full border border-[var(--color-border)] text-sm font-medium text-[var(--color-text)] bg-white/60 hover:bg-white hover:border-[var(--color-accent)] hover:text-[var(--color-accent)] transition-colors shadow-sm"
            >
              <span>{link.label}</span>
              <span className="text-xs opacity-60">{link.icon}</span>
            </a>
          ))}
        </nav>
      )
    }
    ```

    **5. Replace `app/page.tsx`** with the composed layout:
    ```typescript
    import { Avatar } from '@/components/Avatar'
    import { NameCard } from '@/components/NameCard'
    import { LinkList } from '@/components/LinkList'

    export default function Home() {
      return (
        <main className="min-h-screen flex flex-col items-center justify-center px-6 py-16">
          <div className="flex flex-col items-center gap-6 w-full max-w-sm">
            <Avatar />
            <NameCard />
            <LinkList />
          </div>
        </main>
      )
    }
    ```

    After writing all files, run `npm run build` to confirm no TypeScript or build errors. Fix any issues before proceeding to Task 2.
  </action>
  <verify>
    <automated>cd "/Users/joe/Claude Code/Projects/joe-scannell-website" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    Build passes. `components/Avatar.tsx`, `components/NameCard.tsx`, `components/LinkList.tsx`, and `app/page.tsx` all exist. H1 is "Joe Scannell". Tagline is "Founder, Layer One Group". Four links present. Avatar has `priority` prop.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate favicon, apple-icon, and OG image static assets</name>
  <files>app/favicon.ico, app/apple-icon.png, app/opengraph-image.png</files>
  <action>
    Generate three static image assets using ImageMagick or Node.js sharp. These go in the `app/` directory (Next.js file convention -- NOT `public/`).

    **Favicon (`app/favicon.ico`):**
    A simple "JS" monogram (Joe Scannell initials) on a warm off-white background. If ImageMagick is available:
    ```bash
    convert -size 32x32 xc:'#FAF8F5' \
      -font Helvetica-Bold -pointsize 14 -fill '#2C2825' \
      -gravity center -annotate 0 'JS' \
      "/Users/joe/Claude Code/Projects/joe-scannell-website/app/favicon.ico"
    ```
    If ImageMagick is not available, create a minimal valid ICO using a Node.js script with sharp:
    ```javascript
    const sharp = require('sharp');
    sharp({ create: { width: 32, height: 32, channels: 4, background: { r: 250, g: 248, b: 245, alpha: 1 } } })
      .png()
      .toFile('app/favicon.ico');
    ```
    A plain warm-colored square favicon is acceptable -- the goal is a valid file, not a polished icon.

    **Apple touch icon (`app/apple-icon.png`):**
    Same "JS" monogram at 180x180:
    ```bash
    convert -size 180x180 xc:'#FAF8F5' \
      -font Helvetica-Bold -pointsize 72 -fill '#2C2825' \
      -gravity center -annotate 0 'JS' \
      "/Users/joe/Claude Code/Projects/joe-scannell-website/app/apple-icon.png"
    ```
    Or with sharp (180x180 warm background PNG).

    **OG Image (`app/opengraph-image.png`):**
    Static 1200x630 image. Text-based: "Joe Scannell" large, "Founder, Layer One Group" below, warm off-white background:
    ```bash
    convert -size 1200x630 xc:'#FAF8F5' \
      -font Helvetica-Bold -pointsize 72 -fill '#2C2825' \
      -gravity center -annotate +0-40 'Joe Scannell' \
      -font Helvetica -pointsize 36 -fill '#6B6560' \
      -gravity center -annotate +0+60 'Founder, Layer One Group' \
      "/Users/joe/Claude Code/Projects/joe-scannell-website/app/opengraph-image.png"
    ```
    Minimum: A 1200x630 warm-background PNG with legible text. The OG image displays when the page is shared on social.

    After generating all three files, run `npm run build` once more to confirm Next.js picks up the new assets without error.

    Commit and push all changes:
    ```bash
    cd "/Users/joe/Claude Code/Projects/joe-scannell-website"
    git add -A
    git commit -m "feat: add UI components, warm design system, favicon, apple-icon, OG image"
    git push origin main
    ```
  </action>
  <verify>
    <automated>cd "/Users/joe/Claude Code/Projects/joe-scannell-website" && ls app/favicon.ico app/apple-icon.png app/opengraph-image.png && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    `app/favicon.ico`, `app/apple-icon.png`, and `app/opengraph-image.png` all exist. `npm run build` passes. Files are committed and pushed to GitHub. Vercel auto-deploys on push.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify warm personal design and collect SEO inputs</name>
  <what-built>
    Complete UI: Avatar (circular headshot), H1 "Joe Scannell", tagline "Founder, Layer One Group", four equal-prominence pill-button links (Layer One Group, LinkedIn, Twitter/X, Email). Warm off-white background, charcoal text, rounded elements. Favicon showing in browser tab.
  </what-built>
  <how-to-verify>
    1. Open the Vercel subdomain URL from Plan 01-01 SUMMARY in a browser (e.g., https://joe-scannell-website.vercel.app)
    2. Confirm you see: circular headshot, "Joe Scannell" as main heading, "Founder, Layer One Group" below it, and four pill-button links
    3. Open DevTools (F12) > Toggle Device Toolbar > select iPhone SE (375px width) -- confirm no horizontal scroll and the layout stacks cleanly
    4. Confirm the page feels warm and personal, NOT like the Layer One Group site (no glass, no gradients, no purple/pink/green)
    5. Confirm the browser tab shows the favicon

    Also: before approving, provide the following for Plan 01-03:
    - Joe's confirmed LinkedIn URL (e.g., https://linkedin.com/in/[actual-slug])
    - Joe's confirmed Twitter/X URL (e.g., https://twitter.com/[actual-handle] or https://x.com/[handle])
    - Google Search Console verification token for joescannell.com (from GSC property setup -- see STATE.md blocker note)
  </how-to-verify>
  <resume-signal>
    Type "approved" along with the three values: LinkedIn URL, Twitter/X URL, and GSC token. If any are not yet available, type "approved -- [item] TBD" and the SEO plan will use clear PLACEHOLDER markers.
  </resume-signal>
</task>

</tasks>

<verification>
Overall UI checks:
- H1 text is exactly "Joe Scannell"
- Tagline text is exactly "Founder, Layer One Group"
- Avatar uses `priority` prop (required for LCP)
- All four links present: Layer One Group, LinkedIn, Twitter/X, Email (mailto:)
- All external links have `rel="noopener noreferrer"` and `target="_blank"`
- Email link uses `mailto:hello@layeronegroup.com`
- Page is centered, readable at 375px (mobile)
- `npm run build` passes
- Favicon, apple-icon, and OG image files exist in `app/`
</verification>

<success_criteria>
- PAGE-01 through PAGE-08: Page displays headshot, H1, tagline, all four links, mobile-responsive
- DES-01: Warm personal design distinct from Layer One Group
- DES-02: Favicon and apple-icon present
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-and-deploy/01-02-SUMMARY.md` capturing:
- Confirmed Vercel subdomain URL (updated from 01-01 if it changed)
- Design decisions made at Claude's discretion (layout, accent color, link style)
- Confirmed LinkedIn URL (from checkpoint)
- Confirmed Twitter/X URL (from checkpoint)
- Confirmed GSC verification token (from checkpoint)
- Any deviations from planned components
</output>
